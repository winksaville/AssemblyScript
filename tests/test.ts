/// <reference path="../assembly.d.ts" />

class Hello {
  world(param: int): int {
    let a: int = 1;
    while (a) {
      return a;
    }
  }
}

export function add(a: int, b: double): short {
  return (a + (b as int)) as short;
}
